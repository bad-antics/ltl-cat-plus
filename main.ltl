// ltl-cat-plus/main.ltl
//
// `cat` with syntax highlighting, line numbers, and paging.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-cat-plus v0.1.0")
    io::println("`cat` with syntax highlighting, line numbers, and paging.")
    0
}
