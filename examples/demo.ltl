// examples/demo.ltl — runnable demo for ltl_cat_plus

import std::io
use ltl_cat_plus as lib

fn main() {
    io::println("ltl_cat_plus demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}